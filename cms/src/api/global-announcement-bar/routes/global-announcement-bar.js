'use strict';

/**
 * global-announcement-bar router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::global-announcement-bar.global-announcement-bar');
