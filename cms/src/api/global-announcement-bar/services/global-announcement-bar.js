'use strict';

/**
 * global-announcement-bar service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::global-announcement-bar.global-announcement-bar');
